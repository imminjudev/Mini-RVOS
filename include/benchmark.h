#ifndef BENCHMARK_H
#define BENCHMARK_H

#ifdef BENCHMARK_MODE

#ifndef BENCHMARK_QUANTUM_TICKS
#define BENCHMARK_QUANTUM_TICKS 100000UL
#endif

#ifndef BENCHMARK_SWITCHES
#define BENCHMARK_SWITCHES 100UL
#endif

#ifndef BENCHMARK_USE_ASID
#define BENCHMARK_USE_ASID 0
#endif

#ifndef BENCHMARK_WORKLOAD_MEMORY
#define BENCHMARK_WORKLOAD_MEMORY 0
#endif

#ifndef BENCHMARK_WORKING_SET_PAGES
#define BENCHMARK_WORKING_SET_PAGES 1UL
#endif

#define BENCHMARK_PAGE_SIZE 4096UL
#define BENCHMARK_WORKING_SET_BASE 0x41000000UL

#if BENCHMARK_USE_ASID != 0 && BENCHMARK_USE_ASID != 1
#error "BENCHMARK_USE_ASID must be 0 or 1"
#endif

#if BENCHMARK_WORKLOAD_MEMORY != 0 && BENCHMARK_WORKLOAD_MEMORY != 1
#error "BENCHMARK_WORKLOAD_MEMORY must be 0 or 1"
#endif

#if BENCHMARK_WORKLOAD_MEMORY

#if BENCHMARK_WORKING_SET_PAGES != 1 && \
    BENCHMARK_WORKING_SET_PAGES != 8 && \
    BENCHMARK_WORKING_SET_PAGES != 32 && \
    BENCHMARK_WORKING_SET_PAGES != 128
#error "memory working set must be 1, 8, 32, or 128 pages"
#endif

#define BENCHMARK_REPORTED_WORKING_SET_PAGES \
    BENCHMARK_WORKING_SET_PAGES

#else

#define BENCHMARK_REPORTED_WORKING_SET_PAGES 0UL

#endif

#endif

#endif
